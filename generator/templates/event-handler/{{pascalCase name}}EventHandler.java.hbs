package com.rickihastings.cleanarchitecture.application.eventhandlers;

import an.awesome.pipelinr.Notification;
import com.rickihastings.cleanarchitecture.domain.events.{{pascalCase name}}Event;
import lombok.RequiredArgsConstructor;
import org.slf4j.Logger;
import org.springframework.stereotype.Component;

@Component
@RequiredArgsConstructor
public class {{pascalCase name}}EventHandler implements Notification.Handler<{{pascalCase name}}Event> {

    private final Logger logger;

    @Override
    public void handle({{pascalCase name}}Event notification) {
        logger.info(String.format("Domain Event: %s", notification.item.toString()));
    }
}
