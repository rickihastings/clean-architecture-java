package com.rickihastings.cleanarchitecture.application.{{boundary}}.commands.{{strip name}};

import an.awesome.pipelinr.Command;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.rickihastings.cleanarchitecture.application.common.annotations.Authenticate;
import com.rickihastings.cleanarchitecture.application.{{boundary}}.{{pascalCase (getDto name)}};
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;
import javax.validation.constraints.NotBlank;
import javax.validation.constraints.NotNull;

@Getter
@Setter
@NoArgsConstructor
@Authenticate
public class {{pascalCase name}}Command implements Command<{{pascalCase (getDto name)}}> {
    @JsonProperty("title")
    @NotNull(message = "title cannot be null")
    @NotBlank(message = "title cannot be blank")
    private String title;
}
